# Factory AI API密钥配置
# 支持多个密钥，使用分号(;)、逗号(,)或空格分隔，系统会自动轮询使用
FACTORY_API_KEYS=fk-your-api-key-1;fk-your-api-key-2;fk-your-api-key-3

# 代理访问控制（可选）
# 如果设置了此项，客户端需要在请求头中提供匹配的密钥才能访问
# 留空则不启用访问控制
PROXY_ACCESS_KEYS=your-proxy-key-1,your-proxy-key-2

# 代理密钥HTTP头名称（可选，默认: X-Proxy-Key）
PROXY_KEY_HEADER=X-Proxy-Key

# 服务端口（代码中硬编码为8001，此配置项暂未使用）
PROXY_PORT=8001

# 调试模式（可选）
DEBUG_UPSTREAM=true